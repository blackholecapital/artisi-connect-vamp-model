VAMPYR-STAGE1-001 | CF-TEST | Worker A handoff

Result
- PATCH

Status
1) Deployable Vite scaffold added at apps/web-public with package.json, index.html, and src/main.ts.
2) Stage 1 shell is mounted through the existing stage1 mobile navigation/render modules.
3) Cloudflare Pages expected settings are now valid for root=apps/web-public and output=dist.

Blockers
1) Local build dependency install failed in this environment:
   - npm install (apps/web-public) -> 403 Forbidden fetching vite from registry.npmjs.org.
2) Because install failed, local dist generation could not be completed in this sandbox.
3) Cloudflare dashboard/preview URL verification is still out-of-scope for this environment.

Required handoff
- Re-run from an environment with npm registry access, then run:
  - cd apps/web-public
  - npm install
  - npm run build
- Proceed with Cloudflare Pages deploy + route/screenshot verification.
