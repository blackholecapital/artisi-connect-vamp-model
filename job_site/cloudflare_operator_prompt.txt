VAMPYR-STAGE1-001 | CF-TEST | CLOUDFLARE OPERATOR PROMPT (REISSUE)

Objective
- Correct Cloudflare Pages build root mismatch and redeploy Stage 1.
- Verify public routes: /, /discover, /matches, /inbox, /profile.

Required references
1) /MOBILE-CHASSIE-V1-FROZEN-/job_site/cloudflare_prep_report.txt
2) /MOBILE-CHASSIE-V1-FROZEN-/job_site/mobile_demo_runbook.txt

Cloudflare Pages settings (apply exactly)
- Framework preset: None
- Root directory: apps/web-public
- Build command: npm install && npm run build
- Build output directory: dist
  - If build emits a different folder inside apps/web-public, record the actual folder and update Pages setting to match.

Root-cause note
- Prior failure was caused by Pages building from repo root, where npm could not find /package.json.
- Build must run from apps/web-public.

Operator execution steps
1) Confirm repository and working root
   - cd /MOBILE-CHASSIE-V1-FROZEN-
   - Record `pwd`.

2) Confirm app module path before deploy
   - Run from repository root:
     - `ls -la apps/web-public`
     - `test -f apps/web-public/package.json && echo FOUND_PACKAGE_JSON`
   - Record terminal output proving package.json exists under apps/web-public.

3) Apply Cloudflare Pages configuration
   - In Pages project settings, set:
     - Framework preset = None
     - Root directory = apps/web-public
     - Build command = npm install && npm run build
     - Build output directory = dist (or actual verified output under apps/web-public)
   - Save settings.

4) Re-run deploy
   - Trigger a fresh deploy after settings update.
   - Record successful build log lines and preview URL.

5) Public route verification on preview URL
   - Verify and capture screenshots + status for:
     - /
     - /discover
     - /matches
     - /inbox
     - /profile

6) Runtime behavior check (runbook aligned)
   - Confirm non-blank shell render on /discover, /matches, /inbox, /profile.
   - Record observed ready/empty/loading/error notes where available.

7) Evidence recording
   - Save build-log evidence and route screenshots under /MOBILE-CHASSIE-V1-FROZEN-/job_site.
   - Update /MOBILE-CHASSIE-V1-FROZEN-/job_site/cloudflare_test_evidence_index.txt with exact filenames/paths.

Pass criteria
- Cloudflare Pages root directory is apps/web-public.
- Build succeeds from apps/web-public and preview URL is generated.
- Required routes verified with evidence.
- Evidence index updated with build + route artifacts.
